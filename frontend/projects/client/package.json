{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "openapi-generator-cli generate -i http://localhost:5108/swagger/v1/swagger.json -c config.json -g typescript-fetch -o src"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.18.4"
  }
}
