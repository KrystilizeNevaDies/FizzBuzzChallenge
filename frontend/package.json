{
  "name": "fizzbuzz-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn workspace pages dev",
    "build": "yarn workspace pages vite build",
    "preview": "yarn workspace pages vite preview"
  },
  "workspaces": [
    "projects/*"
  ]
}
